{
	"version": 2,
	"installCommand": "yarn install",
	"buildCommand": "yarn build",
	"builds": [
		{
			"src": ".src/server.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/server.js"
		}
	]
}
