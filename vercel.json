{
	"version": 2,
	"installCommand": "yarn install",
	"buildCommand": "yarn build",
	"builds": [
		{
			"src": "./server.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/server.js"
		}
	]
}
