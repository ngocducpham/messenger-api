{
	"version": 2,
	"installCommand": "yarn install",
	"buildCommand": "yarn build",
	"builds": [
		{
			"src": "./app.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/app.js"
		}
	]
}
